server:
  port: 8080
  base_url: http://localhost:8080

spring:
  messages:
    basename: lang.messages
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/SocialNetwork?createDatabaseIfNotExist=true
      &allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true
      &characterEncoding=UTF-8&serverTimezone=UTC
    username: bestuser
    password: bestuser
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  mail:
    host: smtp.gmail.com
    port: 587
    username: jpro021social@gmail.com
    password: SocialNetwork021
  flyway:
    enabled: true
registration:
  verification:
    url: localhost:8080/api/v1/account/register/complete
recovery:
  pass:
    verification:
      url: localhost:8080/api/v1/account//password/recovery/complete
jwt:
  secret: secret