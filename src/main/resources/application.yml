server:
  port: 8080

spring:
  messages:
    basename: lang.messages
  profiles:
    active: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    url:
    username:
    password:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
  mail:
    host:
    port:
    username:
    password:
  flyway:
    enabled: true
jwt:
  secret: secret